import { ElementStates } from "./element-states";

export type TCircle = {
  letter?: string;
  index?: number;
  state?: ElementStates;
}

export type TInput = {
  word?: string;
  fiboInput?: string;
  stackInput?: string;
  queueInput?: string;
  listValue?: string;
  listIndex?: string;
}